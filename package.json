{
  "name": "fisicalc",
  "version": "0.1.0",
  "private": true,
  "description": "FisiCalc — suite de física interactiva (monorepo)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:web": "pnpm --filter @fisicalc/web dev",
    "build:web": "pnpm --filter @fisicalc/web build",
    "test": "pnpm -r test"
  }
}
